# ===============================
# Database Configuration (Render)
# ===============================
spring.datasource.url=${spring.datasource.url=jdbc:mysql://localhost:3306/student_management_system?useSSL=false&serverTimezone=UTC}
spring.datasource.username=${root}
spring.datasource.password=${Goel@@123}

# ===============================
# JPA / Hibernate Config
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ===============================
# Server Config (Render dynamic port)
# ===============================
server.port=${PORT:8080}
